---
タイトル: AGENTS.md作成・OAuth認証情報取得・本実装フェーズ移行
日時: 2026-06-02T15:07:58
sessionID: ${CLAUDE_SESSION_ID}
agent: Claude Sonnet 4.6
branch: feat/phase2-mvp-scaffold
前回ログ: 2026-05-26T16-45-44-agent.md
---

## 重要な決定

**AGENTS.md の設計方針**
`@docs/` を参照しなくてもagentが実装できる最小コンテキストに絞った。
コードを見れば分かること（interface定義・型・エラークラス）は書かない。
権威序列・モノリポ構成・実装制約・ブランチ戦略・TDD規約・コマンドのみ記載。

**docs/ の整理**
元の仕様文書（WORKFLOW.md / PROPOSAL.md / SPEC.md / DESIGN.md / MEMO.md）は削除せず `docs/human/` に移動した。shimが参照できるよう残す判断。agentは AGENTS.md を使う。

**OAuth 認証方式の選択**
Drive API の認証は Service Account ではなく OAuth 2.0 リフレッシュトークン方式を採用。個人アカウント利用のため、Service Accountより設定が少ない。

**本実装フェーズへの移行**
shimの意向で「MVPはモックで完了」ではなく「今すぐ本実装」に方針変更。
gemini-flash TTS と Google Drive API の本実装を次の作業とする。

## 未完了・積み残し

- TTS 本実装（`packages/tts/src/gemini-tts.ts`）未着手
- Drive 本実装（`packages/drive/src/google-drive.ts`）未着手
- `orchestrator/src/main.ts` の DI 切り替えロジック未実装（現在モック固定）
- `config/podcast.config.ts` の `apiProvider.tts` が `'mock'` のまま

## 次sessionへの引き継ぎ

`.env.local` に全環境変数が揃っている状態：
- `GEMINI_API_KEY` ✅
- `GOOGLE_CLIENT_ID` / `GOOGLE_CLIENT_SECRET` / `GOOGLE_REFRESH_TOKEN` ✅
- `DRIVE_FOLDER_ID` ✅

次sessionでやること：
1. `packages/tts/src/gemini-tts.ts` — `GeminiTtsService implements TtsService` 実装
2. `packages/drive/src/google-drive.ts` — `GoogleDriveService implements DriveService` 実装
3. `orchestrator/src/main.ts` — `config.apiProvider` で Mock/Gemini を切り替える DI ロジック追加
4. `config/podcast.config.ts` — `apiProvider.tts: 'gemini'`、`drive: 'google'` に変更
TDD規約に従い test → impl の順で進める。

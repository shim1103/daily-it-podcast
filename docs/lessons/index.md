# lessons

- 2026-09-07 [fix/playback-e2e-test] 障害調査で「最有力の原因」を宣言する前に、その候補を確定 or 棄却できる一次観測（server log・実 request の応答 body）を先に取る。候補を確度順に並べる作業と、決定的な log を 1 本引く作業では後者を優先する。外形（test の失敗 locator）だけで原因を推論して確度を付けると、実 log が別方向を指したとき（例: session 生存を示す header が付いていた）に手戻りする  # → layer:workflow
- 2026-09-07 [fix/playback-e2e-test] 外部 API 認証の失効を疑うとき、失効の HTTP status を仕様で確認してから犯人を名指しする。token 失効は 401、request 内容不正は 400 のように意味が分かれる。status を見ずに「token 切れ」と断定すると、同じ status を返す別原因（設定値の不正など）と混同する。加えて、その API client の共通 request 経路が複数種類の呼び出しを 1 つの error message へ畳んでいると、status だけでは呼び出し種別を切り分けられない。畳み込み経路には呼び出し種別の label を必須引数で持たせ、log から一次切り分けできる粒度にする  # → layer:terms
- 2026-09-07 [fix/playback-e2e-test] flow 指示（例: 「success したら deploy → commit → …」）の実行中に分岐点へ来ても、選択肢が git 履歴で復元可能な範囲なら user へ質問せず自律判断で進める。質問して turn を止めてよいのは、判断材料が本当に不足し、かつ不可逆な副作用に関わるときだけ。flow の完遂責任を「確認を挟む」で薄めない  # → layer:workflow
- 2026-09-08 [feature/generator-text-writer-fallback] 定数（外部 API の model 名・endpoint・閾値）の値は宣言 1 箇所を SSOT にし、docs（Decision・進捗 index・lane）はその識別子を参照するだけにする。docs に値そのものを写すと、版更新のたびに複数箇所の追随が要り、片方だけ古くなる。「なぜその値か」の理由は docs に残してよいが、値そのものは残さない  # → layer:terms
- 2026-09-08 [feature/generator-text-writer-fallback] 委譲先（subagent）の self-report や、過去の別 session が残した commit message の主張を、最終状態の根拠にしない。「消えたのは事故」「union で復元した」等の要約は当時の判断であって事実の確定ではない。conflict や欠損を解消する前に、その path の全履歴を遡り、各 commit の diff と親を実際に見て、誰がいつ何行をどう変えたかを一次情報で組み立てる  # → layer:workflow
- 2026-09-08 [feature/generator-text-writer-fallback] 3-way merge で片側が「ほぼ空のファイル」を起点に大量 append、もう片側も別内容を大量 append していると、ほぼ全行が conflict になる。行数の大きな乖離（253 行 vs 5 行等）を見たら「単純 union で全部残す」を反射的に選ばず、まず merge-base 時点のファイル内容を確認する。base が空なら、どちらかの系列が過去に内容を失っている可能性があり、union は失われた内容を復活させる副作用を持つ  # → layer:platform
- 2026-09-08 [feature/generator-text-writer-fallback] 外部サービスの仕様（利用可能な model・API バージョン・料金）を答える前に、自分の知識の cutoff を意識し、公式 docs / API の一次確認を後回しにしない。「一般に GA な一覧」は知識で言えても「この key/project でその識別子が解決するか」は動的で、API に問い合わせるか公式ページで裏を取るまで確定しない。古い識別子は 404/NOT_FOUND として現れ、smoke test がそれを検出する  # → layer:platform
- 2026-09-08 [feature/generator-text-writer-fallback] 「この失敗は原因 X だから対処 Y にすべき」と分岐条件を決める前に、X を確定 or 棄却できる一次観測（エラー応答の body・code）を取る。取るための診断（応答本文の bounded snippet を error に載せる等）を先に実装してよい。外形（HTTP status だけ）で条件を決めると、同じ status の別原因を取りこぼす。status の意味が仕様上分かれる場合はなおさら body の code まで見て分岐する  # → layer:terms
- 2026-09-08 [feature/manuscript-draft-limits-tune-and-diagram-update] user が示した数式・数値の内訳（「合計 = a + b + c」形式）は、採用して固定する前に自分で検算する。桁・係数のズレはそのまま実装へ入ると後続の不変条件 test を壊す。user の意図（どの値を固定し、どの値に端数を吸収させたいか）は汲みつつ、辻褄の合う組み合わせへ補正して確認する  # → layer:workflow
- 2026-09-08 [feature/manuscript-draft-limits-tune-and-diagram-update] 事前に伝えられた non-scope 宣言と、その後の明示指示が衝突したら、後続の明示指示を優先する。non-scope は初期の想定であって不可侵の壁ではない。「round な目標値を固定し、割り切れない端数を隣の項（non-scope 側）で吸収してよいか」のように、制約の窓を動かせる範囲を先に検討してから代替案を出す  # → layer:workflow
- 2026-09-08 [feature/manuscript-draft-limits-tune-and-diagram-update] 図の node の icon を差し替えるとき、元の icon が担っていた識別情報（言語・framework・役割）が新しい icon で消えていないか確認する。並列する複数の技術（build tool と描画ライブラリ等）を 1 node へ畳むと、片方の存在が図から読み取れなくなる。対等な要素は別 node + 別 icon で並べる  # → layer:terms
- 2026-09-08 [feature/manuscript-draft-limits-tune-and-diagram-update] 構成図のエッジに protocol や機構のラベル（"RPC" 等）を付ける前に、その境界の route 定義を読んで経路が単一か複数かを確認する。同じ 2 コンポーネント間でも、型付き RPC で叩く経路と、メディアを素の HTTP GET（Range 部分応答等）で取る経路が併存することがある。単一ラベルは実装より単純に見せて誤読を生む  # → layer:terms
- 2026-09-08 [feature/manuscript-draft-limits-tune-and-diagram-update] 構成図で片方のスタックにだけ実装言語の node を置くと非対称になり、「もう片方は言語未指定」と読める。framework の node があっても言語は一意に決まらない（JSX は JS でも書ける等）。対になるスタックには同じ粒度で言語 node を置くか、両方省くかで揃える  # → layer:terms
- 2026-09-08 [feature/manuscript-draft-limits-tune-and-diagram-update] 図の複数 node が同系色の icon で隣接すると、別コンポーネントでも同一物と読まれる。役割が異なるものは色を分ける。加えて、実行基盤とその上で動く framework のように包含関係にあるものは、片方を省略せず sub-cluster で階層を示す  # → layer:terms
- 2026-09-08 [feature/generator-gemini-textwriter-prompt-fix] 確定値定義層（定数・対応表・prompt template 等、値そのものが SSOT の module）に対し、その値や本文の一部を assert へ写経して存在確認する test は書かない。値・文面が変わるたび test も書き換える二重管理になり、検出力を持たない。値の正しさはそれを消費する処理（分岐・変換・埋め込み）の test で間接検証する。template なら「消費側が要求する placeholder が解決される」等の構造契約だけを test する  # → layer:terms
- 2026-09-08 [feature/generator-gemini-textwriter-prompt-fix] template（prompt・設定ひな形）へ閾値やマージンの数値を直書きしない。数値は正本の定数から供給される placeholder だけを置く。`{{X}} + 400` のような式を書いても展開器がそれを評価しなければ生文字列のまま下流へ渡る。マージンが要るなら「下限そのものではなく目安値を下限として扱わせる」等、正本と連動する既存 placeholder で表現する  # → layer:terms
- 2026-09-08 [feature/generator-gemini-textwriter-prompt-fix] loop を持たない一発生成の LLM に出力制約を守らせるには、制約の再掲を増やすより「手順 → 各項目の直し方 → 完了条件（全項目が真なら出力、偽なら手順へ戻る）」を明示した自己修正 loop を prompt 内に 1 つ置く。散在する Self-check と修正指示を統合して DRY にし、完了条件を検査可能な述語の集合にする  # → layer:terms
- 2026-09-08 [feature/generator-gemini-textwriter-prompt-fix] LLM は範囲指定の下限・上限ちょうどを狙わせると自分の見積もり誤差で境界を数単位割る／超える。実質の下限・上限を「範囲端」ではなく「目安値」に置き換えて指示すると、出力分布が境界から離れて誤差を吸収できる。範囲端 field ほど分散の裾で不合格になりやすいので優先して目安寄せする  # → layer:terms
- 2026-09-08 [feature/generator-gemini-textwriter-prompt-fix] 課金の伴う外部実行（CI dispatch・API 呼び出し）で prompt や設定を調整するとき、小さな変更ごとに実行して観測するのではなく、観測済みの失敗パターンから複数の修正を 1 回分へまとめてから実行する。実行回数が cost であることを前提に、次の 1 実行で潰せる失敗を全部潰す修正を組む  # → layer:workflow
- 2026-09-08 [feature/generator-gemini-textwriter-prompt-fix] 進捗 index（lane）には完了項目をほぼ書かない。完了は 1 行の要約＋正本（Decision）参照に留め、反復の詳細・実行 ID・失敗と対応の対応表は Decision 側へ寄せる。index が観測ログで膨れると次に読む者が現在地を掴めない  # → layer:workflow
- 2026-09-08 [develop] skill / flow の指示範囲を自分で広げない。「setup だけ」「produce を実行して結果を見る」のように動詞が限定されているとき、その前後の分析・掃除・最適化を気を利かせて足さない。特に破壊的操作（branch 削除・file 削除・履歴書き換え）は、指示に明記が無い限り実行しない。整理したくなったら実行せず候補として口頭報告に留める  # → layer:workflow
- 2026-09-08 [develop] 一次データ（`git log` / `git status` / branch の指す commit / API の応答）を数 turn 前に一度読んだきり、その後の判断でその古い snapshot を根拠にしない。session 中に別 session や hook が状態を進める（background の `git pull` で base branch が fast-forward される等）。分岐を切る・revert する等の履歴操作の直前に、対象 ref の現在値を取り直す  # → layer:platform
- 2026-09-08 [develop] primary provider の実測に合わせて調整した容量パラメータ（生成長・batch size・並列度）は、fallback provider でそのまま安全とは限らない。fallback は往々にして枠の小さい別 tier（free-tier の rate limit 等）で、同じ入力が primary では通っても fallback では枠超過で落ちる。容量を上げる変更は、fallback 経路の枠（TPM/RPD/quota）でも成立するかを実行前に検討し、成立しないなら先に枠側を手当てする  # → layer:terms
- 2026-09-08 [develop] 課金・副作用を伴う本番 workflow を「実行して結果を見る」指示で回すとき、成功/失敗の両分岐で次にやることを先に持っておく。失敗も想定内の分岐であり、失敗 log から原因（どの経路のどの制約に当たったか）を一次情報で特定し、事実として報告して止まる。条件付き後続（「pass したら PR」等）は条件不成立なら実行しない  # → layer:workflow
- 2026-09-08 [develop] 単一 commit を revert したいが後続 commit が同じ file を触っていて `git revert` が conflict するとき、対象 commit が実際に触った file 群だけを `git checkout <commit>^ -- <files>` で親状態へ戻す方が、conflict 解決より確実なことがある。事前に `git log <commit>..HEAD -- <files>` でその file 群に後続変更が無いことを確認すれば clean に戻せる。残りの file（doc 等、後続変更あり）は手で追随する  # → layer:platform
- 2026-09-09 [feature/generator-gemini-key-spare] runtime config の必須項目を 1 つ増やす変更は、値定義（定数・型）だけで閉じず、その config を全項目要求する側（起動時 loader の検証、CI が env を注入する箇所、実 credential 前提の system test の欠落 Skip 判定）へ同時に波及する。値定義だけ足して注入・判定側を放置すると、loader が新項目 missing で落ちて既存の疎通経路が Fatal になる。「この 1 項目を必須にしたとき何が Skip/Fail に変わるか」を先に列挙してから足す  # → layer:terms
- 2026-09-09 [feature/generator-gemini-key-spare] CI の手動実行（workflow dispatch）は、その workflow 定義 file 名が default branch に存在すれば、任意の branch を ref 指定してその branch 側の tree（env・step・script の変更込み）で実行できる。「default branch に変更を merge しないと新しい設定で回せない」は誤り。file 名自体が default branch に無い完全新規 workflow のときだけ、先に定義 file を default branch へ載せる必要がある。ここを取り違えると不要な先行 merge や turn 停止を招く  # → layer:platform
- 2026-09-09 [feature/generator-gemini-key-spare] 変更の「なぜ」を書く場所は、その制約が帰属する境界の契約（型・module の invariant）を 1 次とし、運用手順書はそこを参照するだけにする。実装時に理由を思いつくまま契約側と手順書側の両方へ書くと二重管理になり、後で片方が古くなる。commit 前（または直後の推敲 pass）で "同じ理由を複数 file に書いていないか" を確認し、契約側へ寄せて手順書側を参照へ落とす  # → layer:terms
- 2026-09-09 [feature/generator-gemini-key-spare] 事前に渡された non-scope 宣言（「X は実行しない」）は、session 途中の user 明示指示（「やっぱり X を実行して」）で上書きされる。non-scope は初期の想定であって不可侵ではない。衝突したら後続の明示指示を優先し、上書きに伴って整合が必要になる周辺（その X を回すのに要る前提の追加等）まで含めて完遂する  # → layer:workflow
- 2026-09-09 [feature/generator-gemini-key-spare] 容量超過（rate limit・quota 超過）を、同一資源を共有する 2 つの消費経路の食い合いで起こしている場合、消費側の出力量を絞る対症より、資源そのものを経路ごとに分ける（別 key・別枠・別 pool）方が root cause 除去になる。前者は「片方の消費が多い日」に再発する余地を残す。資源が per-key/per-account で計上されるなら、経路の性質（定常 vs 突発）が違う時点で分離を第一候補にする  # → layer:terms
- 2026-09-10 [feature/generator-logging-settings-update] error 分類を外部境界で行うとき、境界（表示層）に各内部 error の具体型を instanceof で並べさせない。error 値の側に「自分の分類を名乗る」小さな interface を実装させ、境界は interface だけを見る。分類語彙（kind 定数）と分類契約（interface）は最内層が所有し、error 実体は各層が持つ。これで境界の import から内部 package が消え、error 種別追加が境界コード変更を伴わなくなる  # → layer:terms
- 2026-09-10 [feature/generator-logging-settings-update] 同一構造の error 型が Adapter の数だけ増えているとき（保持 field も分類契約の実装も同一で、prefix 文字列だけ違う）、共有 error 型 1 つへ集約し各 Adapter は発生源識別子だけを固定する。ただし集約先は「最内層（ドメイン型の層）」ではない。層のルール上その層に置けるのは分類契約だけで、error 実体は発生源の層内に置く。DRY を理由に層の責務境界を越えない  # → layer:terms
- 2026-09-10 [feature/generator-logging-settings-update] package tree の root 直下には層のディレクトリだけを置く。層に属さない共有 utility・診断 helper・共有 error 型を root 直下の独立 dir にしない。共有物は、それを使う層のディレクトリ直下の非分類区画（platform 名でも汎用名 shared/common/util でもない、責務を名乗る package）へ置く。root 直下の dir 名は層名であって機能名ではない  # → layer:terms
- 2026-09-10 [feature/generator-logging-settings-update] 既存の規約 doc（skill）が現在のコード形（Adapter 1 個前提など）で書かれていて、規模が変わった状況（Adapter 8 個）に規約が答えを持たないとき、規約に反する配置を選ぶ前に規約 doc 自体を更新する。user が「skill がおかしいなら直せ」と言う前提。更新は内部整合（関連セクション相互）と、規約を先に満たしていた別コンテキスト（別 app）を壊さないことを確認してから行う  # → layer:workflow
- 2026-09-10 [feature/generator-logging-settings-update] 言語の埋め込み（embedding / mixin）で method 実装の重複を消すとき、promoted method が埋め込みフィールドを経由する点に注意する。pointer receiver が nil の値に対し promoted method を呼ぶと、フィールドアクセスで panic する。元の手書き実装が receiver 不参照（定数返却のみ）で nil 安全だった場合、embed 化は nil 安全性を後退させる。定数を返すだけの method は DRY 違反の実害（変更追従漏れ）が無いので、手書きのまま複数箇所に置く方が一貫性（nil 安全の対称）を保てる  # → layer:platform
- 2026-09-10 [feature/generator-logging-settings-update] 進捗 log は「開始」と「完了」の 2 点で出す。完了時だけ出すと、長い処理段階が走っている間 log が無言になりハングと区別できず、失敗時はその段階の完了 log が出ないので「どこで落ちたか」が error 表示だけでは弱い。開始 log は処理直前、完了 log は成功判定の後（失敗時は出さない）に置く。処理層は段階名と結果サマリの文字列を観測面へ渡すだけにし、出力そのものは境界 1 点が持つ  # → layer:terms
- 2026-09-10 [feature/generator-logging-settings-update] error message に応答 body を診断目的で載せる変更は、その経路の全 error 生成箇所へ対称に入れる。1 つの status 分岐にだけ body snippet を足して他を素の status 番号のままにすると、後から「なぜここだけ詳しいのか」を問わせる。secret 非露出の根拠 comment（どの header に credential を載せるから body 全出しで安全か）も同じ経路の全箇所に同じ密度で置く  # → layer:terms
- 2026-09-10 [feature/generator-logging-settings-update] 複数の共通型 assert（型で発生源が担保されていた箇所）を単一の共通型へ統合したら、その型を消費する既存の「同種判定」ロジックが発生源を見なくなっていないか確認する。旧コードが型 assert で暗黙に絞っていた条件（この経路の error はこの vendor だけ）が、共通型化で明示条件に落とし込む必要が出る。統合前に型が担保していた不変条件を、統合後は field 比較で明示する  # → layer:terms
- 2026-09-10 [feature/generator-logging-settings-update] 複数の独立した小変更（規約準拠の revert / interface 分化 / doc 修正）を並行 subagent で高速に片付けるとき、各 subagent の担当 file を互いに明示的に排他にする。同じ file を 2 つが触ると片方の変更が失われる。依存のある変更（型定義の変更と、その型を使う側の追随）は 1 つの subagent に束ねる  # → layer:workflow

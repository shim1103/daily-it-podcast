# lessons

- 2026-08-30 [feature/generator-cursorcli-narrow-integration] Sociable Unit から Narrow へ実プロセス／実 I/O 境界の観測を移すと、その production code は SU では原理的に到達できなくなる。Unit coverage を「Unit test だけで計測」する gate だと、移した分がそのまま計測分母から抜けて閾値割れを起こす。coverage は SU + Narrow Integration の合算で production code に対して測るのが一般形。Broad Integration 以上（結線・状態伝播が対象で行を初めて通す test ではない）は分母に入れない  # → layer:terms
- 2026-08-30 [feature/generator-cursorcli-narrow-integration] 「gate の実行対象を分ける」判断（どの test を CI で走らせるか・secret を持たせるか）と「coverage の計測分母を決める」判断は別軸。前者の Decision（例: Integration gate は secret なし Narrow のみ）を後者の根拠に流用しない。走ってよい test の実行結果を coverage 集計へ算入することは、実行対象を増やすことではない  # → layer:terms
- 2026-08-30 [feature/generator-cursorcli-narrow-integration] coverage 閾値割れの対処で「その package を除外リストへ追加」を選ぶ前に、除外方針（実行環境で動かない・framework runtime 依存・外部 service wrapper で実通信必須）に本当に該当するか照合する。実ロジック（環境構築・変換等）を持ち test 環境で実行可能な package を除外すると、そのロジックのバグが gate をすり抜ける。閾値を下げるのも同じく検出力を数値に合わせる操作で不可  # → layer:terms
- 2026-08-30 [feature/generator-cursorcli-narrow-integration] 下位層（command launcher・transport 等）の Narrow test が、検証対象と無関係な上位 vendor Adapter の定数（環境変数名など）を借りて import まで張ることがある。下位層にとってその値は任意でよく、dummy へ置き換えて import を切る。依存方向の逆転は test の import 文に出るので、Narrow を書いたら import が層の向きに沿っているか確認する  # → layer:terms
- 2026-08-30 [feature/generator-cursorcli-narrow-integration] script が別 script の toolchain command 行を固定文字列（`grep -Fq` 等）で検査する CI 契約 test を repo が持つとき、被検査 script の command 行を変えたら needle も同じ commit で追随させる。command を可読性のために複数行分割すると行単位マッチが壊れるので1行に保つ  # → layer:platform
- 2026-08-30 [feature/generator-cursorcli-narrow-integration] 「終わった」と報告する前に、委譲先の self-report を鵜呑みにせず、最終状態を自分の目で確認する（`git diff` 全文・`grep`・gate 再実行）。加えて、その確認コマンド自体が空振り（path 誤り・対象取り違え）していないかを出力から検算する。空の結果を「問題なし」と誤読しない  # → layer:workflow
- 2026-08-30 [feature/generator-cursorcli-narrow-integration] `/pr-completion` のような flow skill は main が skill 呼び出しで自分で進める工程であって、executor へ丸投げする対象ではない。特に Decision Record は既存 Decision を読んで supersede 範囲まで決めた main が書く。manager role でない場面で「edit は全部 executor」を惰性で適用しない  # → layer:workflow
- 2026-08-30 [feature/generator-cursorcli-narrow-integration] executor へ渡すタスクを「Decision + script + 複数 docs」のように性質の違う成果物で束ねない。文書判断（設計判断の記録）と実行可能物（gate script）は所有者・検証手段・レビュー観点が違う。束ねると監査で切り分けにくく、片方の失敗が全体をブロックする  # → layer:workflow
- 2026-08-30 [feature/generator-cursorcli-narrow-integration] 指示形態が session 途中で切り替わる（non-edit Ask → issue-manager flow → /pr-completion 等）とき、直前の role 制約や flow の縛りを次の指示へ持ち越さない。各指示の冒頭で「今どの mode / role か」「edit してよいか」「どの skill の flow に入るか」を読み直してから動く  # → layer:workflow
- 2026-08-30 [feature/generator-su-ni] Narrow Integration の「実物」は検証対象の外部境界 provider（network / TLS 等）を実際に使うこと。custom RoundTripper Spy で request を横取りして応答を合成すると Sociable Unit と同型になり、Scope 名だけ Narrow でも境界 I/O 契約は未検証になる。sibling Narrow（httptest TLS + DialTLS redirect）へ揃える  # → layer:terms
- 2026-08-30 [feature/generator-su-ni] HTTP Adapter の Narrow 成功 path の最小完全集合は「動詞（GET/POST）+ 認証 header 載荷 + 成功成果物」。1つ欠けると偽緑、Adapter 内分岐や field 全 mapping を足すと Unit と二重になる。深い形の正しさは Sociable Unit が所有する  # → layer:terms
- 2026-08-30 [feature/generator-su-ni] Sociable Unit の `httptest.NewRecorder` は RoundTripper Stub の応答合成であり境界到達ではない。DialTLS / NewTLSServer による実到達観測だけを Narrow へ移す。import 名だけで SU 汚染と誤判定しない  # → layer:platform
- 2026-08-30 [feature/generator-su-ni] monorepo の pre-commit が別系統の engine 固定（例: Node 22）を要求するとき、宿主の別 major で npm install すると失敗する。要求 engine の binary を PATH 先頭へ置いてから install し、変更系統の hook を通す  # → layer:platform
- 2026-08-30 [feature/generator-build-manuscript-draft-parse] 「機械判定可能なものはすべて検証する」と決めた Decision の下で parse を実装するとき、検証対象 field を1つずつ Decision の列挙と突き合わせる。似た性質の field 群（朗読 field 等）のうち1つだけ検証を落とす漏れは、実装が通ってしまうため test でも気づきにくい  # → layer:terms
- 2026-08-30 [feature/generator-build-manuscript-draft-parse] test を書く前に、その言語・project の test 構造規約（Given-When-Then の「1 test = 1 仕様 = 1 GWT セット」、境界値は個別 case で列挙）を定める ref を読む。table 駆動で複数の別仕様を1 case・1 GWT コメントへ圧縮するのは規約違反。ref 未読のまま書くと後から全 test を分解し直すことになる  # → layer:workflow
- 2026-08-30 [feature/generator-build-manuscript-draft-parse] 「今の定数値だとこの入力は到達不能だから、その経路の test は書けない／廃止する」という判断をしない。定数は変わる。定数の現在値に依存した test は、定数が変わって検証ロジックが壊れても・定数間の整合が崩れても検知できない。境界値 test は定数から相対的に（`Max + 1` 等）入力を組み、定数がどこへ動いても追随する形にする  # → layer:terms
- 2026-08-30 [feature/generator-build-manuscript-draft-parse] 合計値（複数 field の和）の range 検証を、その和を内部で計算する上位関数（JSON parse 全体）経由でだけ test すると、各 field の range 制約に阻まれて「和だけを狙った値」の入力を作れず、上限側の経路が試せなくなる。和を検証する関数を非公開のまま単体で叩く内部 test を分け、field 制約と無関係に和を任意値へ寄せる  # → layer:terms
- 2026-08-30 [feature/generator-build-manuscript-draft-parse] fixture の健全性ガードを、生成関数の内部ロジックを test 側へ丸写しして検算する形で書かない。DRY 違反で生成関数の変更に追随しない。生成した fixture を実際の検証関数（または parse 本体）へ通して pass することを確認する自己検証 test を1本置く  # → layer:terms
- 2026-08-30 [feature/generator-build-manuscript-draft-parse] non-edit 制約下の role（audit 専任の manager 等）は、審査中に見つけた別スコープの課題を勝手に課題管理領域（todo 等）へ file 化しない。それは編集であり role 違反。既存の進捗 index / lane に既にその課題枠があるならそこへ一言追記するに留め、正式な起票は専用 workflow（create-issue 等）と user 判断に委ねる  # → layer:workflow
- 2026-08-30 [feature/generator-build-manuscript-draft-parse] 委譲先が「◯◯は構造的に不可能なので test を廃止した」と報告してきたら、その「不可能」が恒久的な仕様なのか現在のパラメータ値に由来する一時的なものかを切り分けてから受け入れる。後者なら廃止ではなく test 構造の作り直し（パラメータ追随）が正しい対応  # → layer:workflow
- 2026-08-30 [feature/generator-build-manuscript-draft-parse] 全系統一括の pre-commit/pre-push hook を持つ repo で片系統だけ変更した場合、無変更系統の依存欠如（formatter・test runner の command not found）で hook が落ちる。変更内容の欠陥と切り分け、変更した系統の static/unit/integration を手元で個別に全 pass 確認してから hook を skip して commit する  # → layer:platform
- 2026-08-29 [feature/generator-build-compose-brief] contract stub の panic は「未実装到達点」の観測用であり、本実装の想定内失敗（空入力・前提違反の呼び出し）へ流用しない。既存の Domain Error 識別子があるなら Result 型で返し、panic は invariant 違反や到達不能枝に限る  # → layer:terms
- 2026-08-29 [feature/generator-build-compose-brief] test 名は repo 規約（英語・振る舞い_when_条件）に従い、GWT comment は 1 test case あたり Given / When / Then 1 組だけ置く。複数 Then 節や日本語 test 名は skill 違反であり、review 前に executor 自身が naming-and-layout と gwt を読んで直す  # → layer:workflow
- 2026-08-29 [feature/generator-build-compose-brief] issue-manager が Issue file 削除まで完了した後の user 追加修正は、完了済み flow を再開する指示ではない。role 制約は解けているので、追加指示を独立した修正として受け、品質指摘（error 表現・test 規約）は skill 原文を読んだうえで直す  # → layer:workflow
- 2026-08-29 [feature/generator-build-wav-concat] 自然言語（doc・comment・CLI 表示・test の説明文）は現地語で書くが、code identifier（test 関数名含む）は language・toolchain・project の命名規約に従う、という上位規約の後段を落として identifier まで現地語にしない。両者は別 rule であり、identifier 側は project 内の既存 test の実際の命名を grep で確認してから合わせる  # → layer:meta
- 2026-08-29 [feature/generator-build-wav-concat] 「◯◯と書いてある source/skill/doc があったら直せ」と指示されたら、まず本当にそう書いてあるか grep で確認する。実際には規約は正しく、自分（や委譲先）の解釈が誤っていただけなら、直す対象は doc ではなく生成物。規約改訂と実装修正を取り違えない  # → layer:workflow
- 2026-08-29 [feature/generator-build-wav-concat] 1 test case は 1 仕様・When 1 回。同一関数を別入力で 2 回呼び Then を 3 塊並べたら、主仕様の case と網羅性の case へ分ける。同一対象の複数 property 検証は 1 case 内で許容されるが、別入力・別観点は別 case  # → layer:terms
- 2026-08-29 [feature/generator-build-wav-concat] test case を持たず fixture・helper だけを収める file に、test 発見機構の予約 suffix は外せない言語（Go の `_test.go` 等）でも、file 名の主語は「fixture」「helper」等の役割語にする。「testsupport」のような test を含む名前は case のある file と紛れる。同 package 内に既にある helper 専用 file の命名形式（`<対象>_<役割名詞>_test.go` 等）へ揃える  # → layer:terms
- 2026-08-29 [feature/generator-build-wav-concat] 標準ライブラリで数十行に収まる binary format（RIFF header 等）の parse に外部 library を足さない。依存を増やさない制約がある文脈ではとくに、`encoding/binary` 相当で header 読取と再構築を直書きする方が、library の抽象を学ぶコストより安い  # → layer:platform
- 2026-08-29 [feature/generator-build-wav-concat] pre-commit / pre-push が全系統の static・unit・integration を走らせる monorepo で、片系統（自分が触っていない方）の toolchain 未 install・engine 不整合で hook が落ちるのは環境要因。自分の変更範囲の系統を単独 script で緑と確認したうえで `--no-verify` を使い、理由を報告に残す。hook 失敗を自分の変更の欠陥と誤認しない  # → layer:platform
- 2026-08-30 [feature/generator-manuscript-draft-limits-seconds-model] 「単位 A で決めた値」と「A から係数で導く単位 B の値」があるとき、正本を A に置き B を const 畳み込みで導出する。B を正本にすると係数変更時に全 B 値の手計算が要り、かつ「元々 A でいくつを意図したか」が記録から失われる。係数（発話速度・換算レート等）は将来調整される前提なら特に、派生値を正本にしない  # → layer:terms
- 2026-08-30 [feature/generator-manuscript-draft-limits-seconds-model] 単位変換（秒→文字等）を内側 ring（Entities）の const 畳み込みで閉じれば、外側 ring（Use Cases）は変換後の単位だけを見ればよく、層をまたぐ変換関数が不要になる。「計算の責務はどの層か」と訊かれたら、まず変換を定数畳み込みで消せないか検討する。消せるなら責務の配置問題自体が消える  # → layer:terms
- 2026-08-30 [feature/generator-manuscript-draft-limits-seconds-model] 「各要素の min/max」と「合計の min/max」が両方ある系で、両者を等式（Σ要素min == 合計min）で結ぶと「どれか短め・どれか長めでも全体は範囲内」という融通が消える。両者は独立の窓であり、valid な入力は両方を同時に満たす。整合検証は等式ではなく「最小構成の合計 ≦ 合計上限 かつ 最大構成の合計 ≧ 合計下限」（各要素 range 内で合計 range も満たす組が存在する）という両立可能性で書く  # → layer:terms
- 2026-08-30 [feature/generator-manuscript-draft-limits-seconds-model] 定数群の整合を固定する contract test で、実装の定義式（`A == B * C`）をそのまま assert しない。実装のバグも fixture にコピーされ検出力を持たない自己参照 assert になる。検証するのは定数どうしの数学的関係（順序・両立可能性・target 一致）だけにし、個々の値の妥当性はその値を消費する振る舞いの test に委ねる  # → layer:terms
- 2026-08-30 [feature/generator-manuscript-draft-limits-seconds-model] 一覧・見出しに出すが読み上げ・表示尺に寄与しない field（episode タイトル等）を、尺・分量の合計対象に含めない。含めると「本文を削って見出しを伸ばしても尺が変わらないのに合計だけ増える」歪みが出る。見出し用の検証は本文用（末尾句点必須等）から分け、見出しに不自然な制約（文末の句点）を課さない  # → layer:terms
- 2026-08-30 [feature/generator-manuscript-draft-limits-seconds-model] prompt・template の固定文字列に、別の場所（定数）と同義の数値をハードコードしない。placeholder 化して定数から埋め、SSOT を一本化する。「約 N 分」のような補足数値でも、対応する定数があるなら二重管理を避ける  # → layer:terms
- 2026-08-30 [feature/generator-manuscript-draft-limits-seconds-model] error message（Error の cause）を現地語で書くか英語で書くかは、その codebase の既存 Error 群の実態に揃える。明示規約が無くても、既存が全て英語なら一貫性（Least Astonishment）が明示規約に優先する。cause は内部診断表現でありユーザー向け表示ではない点も英語寄りの根拠になる  # → layer:terms
- 2026-08-30 [feature/generator-manuscript-draft-limits-seconds-model] test 名の言語規約が上位（現地語）と個別指示（英語）で食い違ったら、規約 doc を「test名はすべて英語。名前でない自然言語（GWT comment・assert message）は現地語」の形へ更新してから実装を直す。philosophy の「規約 doc と現在の指示が食い違う時は指示を優先し規約を後続で整合させる」を実行する  # → layer:workflow
- 2026-08-30 [feature/generator-manuscript-draft-limits-seconds-model] 自分が前 session で「復元」した内容が、その後の user の autosave commit で再び消えていたら、それは事故ではなく user の意図（消すべきものを消した）である可能性を先に疑う。append-only 台帳でも、user が整理・削除した版が最新なら、自 branch がたまたま持つ旧版を「復元」として持ち込まず、最新版へ揃える  # → layer:workflow
- 2026-08-30 [feature/generator-manuscript-draft-limits-seconds-model] commit を意味単位で分ける指示のとき、`git add <明示path>` の前に `git status` で index に既に staged なものがないか確認する。前段の作業（subagent の git mv 等）で index へ入った無関係 file が、次の commit へ黙って混入する  # → layer:workflow
- 2026-08-30 [feature/playback-list-episodes-audio-ref] manager が edit してよいのは Decision・調査・委譲・監査まで。実装差分は executor へ渡す。manager が直 edit すると「話を聞かない」「squad を飛ばす」と同じ失敗になる  # → layer:workflow
- 2026-08-30 [feature/playback-list-episodes-audio-ref] 「共有資源をどこに置くか」を page 直下へ逃がすと、一覧全体の上に1つある見た目になり、行との対応が消える。資源の宿主は「どの domain 行に属するか」が読める位置へ置く  # → layer:terms
- 2026-08-30 [feature/playback-list-episodes-audio-ref] Feature が ViewModel hook を所有すると、列挙だけの Feature が状態管理を抱え SRP が崩れる。Page が hook を呼び、Feature は props で描く  # → layer:terms
- 2026-08-30 [feature/playback-list-episodes-audio-ref] 未完成の UI 設計を API 契約変更と同じ PR に混ぜると、review が設計欠陥を契約差分として固定してしまう。完成した契約だけを先に切り、未完成 UI は別 branch / WIP に残す  # → layer:workflow
- 2026-08-30 [feature/playback-list-episodes-audio-ref] `null` と boolean を混ぜた props は「空」と「偽」が区別できず、discriminated union で明示した意図が崩れる。空は名前付き variant で表す  # → layer:terms
- 2026-08-30 [feature/generator-composition-produce-episode-wiring] 達成契約 file が「Factory を実装する」と書いていても、それだけを根拠に型 + ctor + method を足さない。まず対象コードの分岐・状態を見て、関数 1 つで足りるか KISS で検証する。組み立て関数に分岐も状態もなく ctor を順に呼ぶだけなら factory は死んだ中間層になる。契約 file の記述は「何を達成するか」であって「どの構造で書くか」まで縛らない  # → layer:terms
- 2026-08-30 [feature/generator-composition-produce-episode-wiring] 「SU unit と実装 file を 1対1 対応させる」は「分岐・状態を持つ file だけが対応 test file を持つ」の意味であって「全実装 file が test file を持つ」ではない。分岐のない結線・DI 配線・ctor 呼び出しだけの file は test を持たず build と上位経路の compile で守る。結線層に「生成物が非 nil」を assert する test を置くのは、分岐のない結線への構造 guard であり振る舞い検証ではない  # → layer:terms
- 2026-08-30 [feature/generator-composition-produce-episode-wiring] コード配置場所（package 名・層名）から test 分類を決めない。分類軸は Scope × Sociability であって置き場所ではない。「Composition Root 配下だから Integration が要る」ではなく、その対象が外部境界（network / disk / DB / 外部 process）を持つかで Narrow の要否を判断する。外部境界を持たない純関数の合成は Sociable Unit で足りる  # → layer:terms
- 2026-08-30 [feature/generator-composition-produce-episode-wiring] 「自然言語は現地語・code identifier は言語/toolchain/project 規約」の上位規約で、test 関数名は identifier 側。査読者が「この package では現地語命名が規約準拠」と言い出したら、まず repo 内の既存 test 名を grep で確認する。既存が全て英語なら一貫性（Least Astonishment）が優先し、現地語 test 名は違反。前 session でも同型の指摘が出ている再発項目  # → layer:workflow
- 2026-08-30 [feature/generator-composition-produce-episode-wiring] issue-manager が契約 file 削除まで一巡した後の user 追加指示（factory 撤回・test 再構成）は、閉じた flow の蒸し返しではなく独立した修正依頼として受ける。前段の実装・査読が契約 file の記述に引きずられて過剰構造を通していたら、その撤回も追加指示の正当な範囲  # → layer:workflow
- 2026-08-30 [feature/generator-produce-episode-run] non-edit の manager が委譲先へ渡す指示に「勝手な文案・値を作れ」と読める余地を残さない。user note の「◯◯書いたから test してね」は「値は既に確定済み、テストだけ足せ」の意味であって「値を考えて書け」ではない。指示を出す前に対象ファイルの現在値を実際に読み、確定済みかどうかを確認する  # → layer:workflow
- 2026-08-30 [feature/generator-produce-episode-run] user が明示的に書いた定数値（空文字含む）を、委譲先が「未決だから埋めた」と解釈して別の値へ変えるのは、明示 input の書き換えであり許可なし編集。空文字や未実装スタブも「user がそう書いた状態」であり、テスト対象として尊重する。委譲先の成果物を監査するとき、user が直接書いた行が変わっていないかを差分で確認する  # → layer:workflow
- 2026-08-30 [feature/generator-produce-episode-run] 非決定な依存（乱数由来の ID 発行・実時刻・タイムゾーンの tzdata 解決）は Use Case 層に実体を置かず、Composition Root（または entrypoint）が production 既定値として供給し、Use Case は解決済みの値・関数として注入で受ける。entrypoint が実時刻を引数で渡すのと同型。Use Case 内の helper は注入された値だけを使う純関数に保ち、外部リソース解決の I/O と失敗経路を持ち込まない  # → layer:terms
- 2026-08-30 [feature/generator-produce-episode-run] tzdata / zoneinfo の読み込みは disk I/O であり、minimal container 等で失敗しうる。表示タイムゾーンの解決を Use Case の実行フロー中で行うと、Infra 障害が Domain フローに混ざり、実行途中で「タイムゾーンが引けない」で落ちる。解決は起動時に Composition で 1 回行い、失敗は起動前 panic で顕在化させる  # → layer:platform
- 2026-08-30 [feature/generator-produce-episode-run] 「呼び出し側のバグだから panic」は Use Case / helper 層の throw 規約に反する。内部前提違反（集約処理の要素数不一致等）でも、その層が能動的に返すのは Domain Error のみ。専用 Op を errors パッケージへ足し、Result 型で返す。panic は本当に到達不能な枝に限る  # → layer:terms
- 2026-08-30 [feature/generator-produce-episode-run] panic を Domain Error へ格下げしたら、対応する test の `recover()` 検証を「戻り err の Op を assert」へ書き換え、test 名も `_panics_when` から `_returns<Op>_when` へ変える。recover ベースの test を残すと、実装が error 返却に変わっても panic 前提のまま緑になり得る  # → layer:terms
- 2026-08-30 [feature/generator-produce-episode-run] 既に別 helper（brief 組み立て等）が空入力で Domain Error を throw しているなら、それを呼ぶ上位 Use Case は同じ 0 件チェックを自前で再実装しない。既存 throw をそのまま propagate すれば「0 件なら書き込まない」が自然に成立する。同じ判定を 2 箇所に置くと片方が腐る  # → layer:terms
- 2026-08-30 [feature/generator-produce-episode-run] 引数で任意の値（`*time.Location` 等）を受け取る helper に、特定の値を前提とした名前（`jstDate` 等）を付けない。名前は「何をするか」（`displayDate`）を表し、渡される具体値に縛られない。private helper でも次の読み手が誤読する  # → layer:terms
- 2026-08-30 [feature/generator-produce-episode-run] non-edit の manager でも、PR completion フロー（commit / log-session / create-pr）に入ったら軽微な rename 等の直接編集は手順の一部として実施してよい。audit 専任の制約は実装フェーズのもので、user が completion を指示した後の仕上げ編集は範囲内。ただし新規ロジックは依然 executor へ渡す  # → layer:workflow
- 2026-08-30 [docs/generator-broad-system-e2e-plan] 後続 Decision が前提（local secret 供給等）を消したら、字面が残る先行 Decision をそのまま適用しない。前提が消えた範囲は新 Decision で置き換え範囲を書き、旧本文は触らない  # → layer:meta
- 2026-08-30 [docs/generator-broad-system-e2e-plan] Go の build tag は「tagged file を足す」だけで、同一 package の untagged test は -tags 実行時も残る。tagged suite だけ回す入口は別 package（または !tag）で隔離する  # → layer:platform
- 2026-08-30 [docs/generator-broad-system-e2e-plan] 地図・受け入れは外向き文書、層と test 規則は設計文書、secret 登録と定時運用は運用文書へ分ける。同じ inventory を複数 root 文書へ写さない  # → layer:meta
- 2026-08-30 [feature/generator-broad-integration-produce-episode] Broad Integration が所有するのは複数 production 実装の配線・状態伝播・error 伝播であって、env 読取や Config load の entrypoint 関数そのものではない。真外部を DialTLS / fake child で差し替える必要があり shared client に注入口が無いなら、同型 Adapter を手組みするのは Broad の正当な形であり「entrypoint 未通過」を欠陥と見なさない  # → layer:terms
- 2026-08-30 [feature/generator-broad-integration-produce-episode] Narrow と Broad が同じ double（TLS redirect・fake CLI・成功 handler）を持つなら、境界名付き helper のまま相乗りせず中立 support へ出す。上位 Scope が下位 file 固有 helper に依存すると層の逆向き結合になる  # → layer:terms
- 2026-08-30 [feature/generator-broad-integration-produce-episode] Broad の wire / fixture は「valid な代表が通る」最小で足りる。下位 Scope が所有する range 網羅（max 長 field 等）を Broad fixture に持ち込むと minimization 境界を越える  # → layer:terms
- 2026-08-30 [feature/generator-broad-integration-produce-episode] issue-manager の途中で委譲 Task が中断しても、既に disk にある成果物を Read で確認し、完了済み step をやり直さず次の role（reviewer 等）から再開する  # → layer:workflow
- 2026-08-30 [docs/playback-integration-e2e-plan] Decision の「1判断1ファイル」は1つの問いへの1答えであり、議論の論点番号をそのまま1 fileへ機械写経する規則ではない。同じ問いの側面は1 file内の複数箇条で足り、細分化しすぎると Reason/Rejected が薄くなり地図が壊れる  # → layer:meta
- 2026-08-30 [docs/playback-integration-e2e-plan] GitHub Actions の Secrets/Variables は登録しただけでは job の process.env に出ない。workflow の env 写像が必要。JSON 本文の Secret を Playwright storageState の path 設定へ渡すときは、step で file へ書き出してから path を env にする  # → layer:platform
- 2026-08-30 [docs/playback-integration-e2e-plan] edge の入場認証（例: メール OTP）と、server 側の外部 API 認可（例: OAuth refresh）は別境界。browser E2E が後者のログイン画面を操作する必要はなく、前者の毎回自動化も必須ではない  # → layer:terms
- 2026-08-30 [feature/playback-gdrive-ni-worker-composition-bi] Composition を通す Broad Integration で use case override を使うと repository 解決を迂回する。真外部だけを double し、設定充足で production 結線を通すのが一般形  # → layer:terms
- 2026-08-30 [feature/playback-gdrive-ni-worker-composition-bi] Broad の成功観測は status・契約 schema・配線を示す代表値に留め、下位 Scope が所有する mapping 完全一致を再 assert しない  # → layer:terms
- 2026-08-30 [feature/playback-gdrive-ni-worker-composition-bi] Adapter が本番 host を定数に持つ Narrow では、host rewrite 後の実 fetch + local listener が DialTLS 相当になる。即 Response 合成は Sociable Unit と同型  # → layer:platform
- 2026-08-30 [feature/playback-gdrive-ni-worker-composition-bi] issue-manager の no-arg は branch 名対応 file の解決規則であり、明示 path で別達成契約を同じ branch で続けてよい。同時に複数 issue file を編集し続けることとは別  # → layer:workflow
- 2026-08-30 [feature/playback-narrow-integration-api-client] local の `origin/<base>` が stale なまま「未実装」と判断して再実装すると、remote で既に merge 済みの同契約と二重になる。再実装や並列 lane 着手の前に `fetch` し、remote tip・merged PR・達成契約 file の有無を照合する  # → layer:workflow
- 2026-08-30 [feature/playback-narrow-integration-api-client] 同契約の成果物が命名だけ違う file として両方残ると Integration gate が二重実行になる。remote 側を正とした merge 後は自前の命名 variant を消し、net diff が契約単位で一意になることを確認する  # → layer:workflow
- 2026-08-30 [feature/playback-narrow-integration-api-client] uncommitted の破棄・stash が runtime policy で拒まれるとき、意味単位 commit の後に base merge で重複を落とす経路を取る。未 commit のまま「なかったこと」にできない制約を、破棄 command の再試行で回さない  # → layer:workflow
- 2026-08-31 [refactor/playback-hooks-components-kiss-slim] 公開面から概念を廃止したあと、同名接頭辞の Request・隣接経路・comment まで残すと「消えた」報告と字列残存が矛盾する。廃止名は入力契約と隣接 identifier も含め部分一致で洗い、歴史記録（decision / daily）だけ意図的に残す  # → layer:terms
- 2026-08-31 [refactor/playback-hooks-components-kiss-slim] 委譲先の完了報告を採る前に、禁止字列や削除対象は報告の「本番経路」主張ではなく repo 全体の部分一致 grep で独立確認する。型名1つだけ見ると兄弟 schema が漏れる  # → layer:workflow
- 2026-08-31 [refactor/playback-hooks-components-kiss-slim] review で「関連が残るから旧名を維持」と凍結した判断は、呼び出し側の指摘で覆りうる。凍結表に残置を書くなら Intention-Revealing と字列禁止の両方を先に照合する  # → layer:workflow
- 2026-08-31 [feature/playback-e2e-deploy] 完成成果物の schema Gate と、上流 Builder の Domain Rule（文字数・句点・件数）は別境界。schema だけ通しても produce 経路では書けない稿になり得る。安定 fixture は上流 validation も通す  # → layer:terms
- 2026-08-31 [feature/playback-e2e-deploy] 完成稿の durationSec は実音声 Timeline の観測値であり、原稿文字数から導いた想定発話尺との一致を schema は強制しない。E2E が尺・シークを契約するまで stub 短尺で足り、必要になったら差し替える  # → layer:terms
- 2026-08-31 [feature/playback-e2e-deploy] remote E2E で credential が無いとき、空 suite や常緑 placeholder ではなく skip で入口を緑に保つ。本物 assert は env があるときだけ実行する  # → layer:platform
- 2026-08-31 [feature/playback-e2e-deploy] Integration と browser E2E は runner・gate・credential 前提が違う。同じ test 直下の平置きより、dir で分け file 名に分類語を残す方が収集境界が読める。未着手の contract 専用 dir を先置きしない  # → layer:terms
- 2026-08-31 [drafts-playback-ops-ssot] OAuth Client ID 欄に Client Secret（GOCSPX- 形）を入れると、設定不足（configuration_error）ではなく外部 token 失敗（unavailable）として見える。注入区分の形を deploy 後に一回照合する  # → layer:platform
- 2026-08-31 [drafts-playback-ops-ssot] Workers の console 出力を Dashboard に残すには observability を有効化して再 deploy が要る。未設定なら wrangler tail のリアルタイムだけ  # → layer:platform
- 2026-08-31 [drafts-playback-ops-ssot] GitHub Actions の workflow_dispatch 対象は default branch に yml があるものに限る。integration branch だけに置いても API/UI から見えない  # → layer:platform
- 2026-08-31 [drafts-playback-ops-ssot] 共通祖先のない2 branch は通常 PR で合流できない。workflow 載せるだけなら default 由来の薄い branch、全量なら unrelated-histories merge か default 切替を選ぶ  # → layer:workflow
- 2026-08-31 [drafts-playback-ops-ssot] 達成契約が実質完了したら checkbox 放置より file 削除と進捗 index 縮約の方が、次 session が「まだ未完」と誤読しにくい  # → layer:workflow
- 2026-09-01 [cursor-docs-ssot-develop-cleanup-9567] obsolete 文書の整理は通常 merge で足りる。git history 改変を同梱すると review 負荷と拒否リスクが上がる  # → layer:workflow
- 2026-09-01 [cursor-docs-ssot-develop-cleanup-9567] 既存 Decision で却下済みの文書形（PROPOSAL/SPEC 等）が物理的に残っていたら、新 Decision を増やさず削除実施として daily に書き、方針は先行 Decision を参照する  # → layer:meta
- 2026-09-01 [cursor-docs-ssot-develop-cleanup-9567] README 地図に rewrite/archive 履歴を書くと、現行 DESIGN・contracts より旧文書へ読者が迷い込む。凍結は git tag に任せ、地図は latest のみ  # → layer:meta
- 2026-09-01 [cursor-docs-ssot-develop-cleanup-9567] 運用 SSOT（DEPLOY 等）の「未実装 caveat」は実装到達後に消す。lane index と矛盾する注意書きを残すと、どちらが正かが二重になる  # → layer:workflow
- 2026-09-01 [cursor-docs-ssot-develop-cleanup-9567] log-session では daily だけでなく、session で新たに固定した方針（history 改変しない・地図から archive を外す等）を Decision に落とし、再発防止の一般則を lessons へ layer 分割して追記する  # → layer:workflow
- 2026-09-02 [feature-hackernews-api-adapter] 「この経路は◯◯できないはず」と答える前に、その tool / runtime の現行仕様を必ず調べる。調べずに能力の有無を断定するのは推測での回答であり、後続の設計判断を誤らせる。不確実なら「未確認」と declare して調査する  # → layer:meta
- 2026-09-02 [feature-hackernews-api-adapter] 情報の「本体」を持つ adapter は、参照 URL を出力に含めても自分では fetch しない。fetch の要否は下流（原稿生成側）が判断する関心事で、adapter に fetch を持たせると変更理由が「取得元 API の変更」と「任意サイトの本文抽出」の2つになり SRP に反する。参照 URL は「本体があり、さらなる背景として URL がある」形で常に載せておく  # → layer:terms
- 2026-09-02 [feature-hackernews-api-adapter] 複数の外部媒体を1つの「汎用 adapter」＋種別リストで捌く設計は、媒体差分を吸収する facade であり「偽の共通概念」。1媒体1 adapter にして識別子・置き場・中身を一致させ、共通化は同種の parse ロジックが三度現れてから検討する。実源が1本の段階で「汎用」化するのは将来投機（YAGNI）  # → layer:terms
- 2026-09-02 [feature-hackernews-api-adapter] 廃止する外部依存は「無効化して結線から外す」で止めず、その依存の識別子・型・CI 設定・test の double・dead な helper まで部分一致 grep で洗って消す。生きていない結線が残ると次の読み手に「これは使うのか」を問わせ続ける。復元は履歴からできるので、破棄と延期を同じ操作にしない  # → layer:workflow
- 2026-09-02 [feature-hackernews-api-adapter] 委譲先が「compile を通す最小修正」に留めた dead code を、監査で「終わった」と受けない。テスト suite が FAIL や false-green を残したまま渡されたら、RED は SKIP（理由付き）へ変換させ、全 green-or-SKIP を hand-off の条件にする。RED を FAIL のまま次工程へ流さない  # → layer:workflow
- 2026-09-02 [feature-hackernews-api-adapter] 契約凍結フェーズで interface stub を作ったら、その stub を叩く test も同時に stub で置く（`t.Skip` に「実装者が組む double と assert 内容」を明記）。stub を panic で叩いて緑になる test は false-green。test 名は本実装が満たすべき契約の各面に対応させ、次工程のチェックリストにする  # → layer:terms
- 2026-09-02 [feature-hackernews-api-adapter] ADR は「変えにくい / 代替があった / 制約が決めた」1つの選択に限る。overarching な決定（A をやめて B へ移す）が sub-decision（B の取得経路は X か Y か、境界は誰が持つか）を生むなら、主決定1 file + 派生を各別 file にする。1 file が1ページを大きく超えたら束ね過ぎを疑う。規約・命名・formatting は ADR の対象外で automation が持つ  # → layer:meta
- 2026-09-02 [feature-hackernews-api-adapter] 契約凍結（A）と決定記録（B）は固定順序ではなく対等な2つの SSOT で、境界は DRY で引く（契約値は A だけ、理由・却下は B だけ）。どちらから書いてもよく行き来して整合させる。「B は A を正にする」を「A を先に完成させないと B を書けない」と読み替えない  # → layer:workflow
- 2026-09-02 [feature-hackernews-api-adapter] 下位 skill が上位 SSOT skill の規則（decision の粒度・厚さ・file 分割）を自前で詳述していたら DRY 違反。下位は「参照する」1文に畳み、規則の本文は上位 skill だけが持つ。上位を改訂したとき下位が古い規則を持ち続ける二重管理を防ぐ  # → layer:meta
- 2026-09-02 [feature-hackernews-api-adapter] monorepo で片系統だけ変更した commit が、無変更系統の toolchain 未 install で pre-commit / pre-push hook に落ちるとき、`--no-verify` で回避しない。要求 engine version の binary を PATH 先頭に置いて依存を install し、hook を実際に緑で通す。hook 失敗を「自分の変更と無関係だから」と読み飛ばさず、gate を本当に通した状態で push する  # → layer:workflow

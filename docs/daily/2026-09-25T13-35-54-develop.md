---
name: branch掃除とagent-standards-setup再配置
date: 2026-09-25T13:35:54
session_id: none
branch: develop
prev: 2026-09-17T16-03-00-develop.md
---

## 1. Summary

local/remote の完了・不要 branch を掃除し、残すのは `master` / `develop` と進行中の `refactor/generator-go-performance` / `docs/playback-audio-history` だけにした。続けて agent-standards-setup で Claude / Codex / Cursor 設定を再配置し、生成物を `.gitignore` へ載せた。branch 残置方針は Decision に固定し、squash / orphan などの用語は lessons へ候補として残した。

## 2. Changes

1. `develop` へ直接 merge 済みの remote tip を一括削除した（pre-push 通過のため playback の依存を `npm ci` で直してから実行）
2. 間接 merge・不要・再開しない tip と、別 branch 経由で integration へ入った `refactor/playback-web-audio-adapter-design` を local/remote から削除した
3. tracked だった `.claude` / `.codex` / `.cursor` 個別設定を外し、`~/settings/agent-standards-setup` から三 tool を `~/projects/daily-it-podcast` へ setup した
4. setup 生成物を `.gitignore` に追加した
5. branch 残置方針を Decision に書き、用語知見を lessons へ追記した

### Commits

1. `241b4fd`
2. `9ed725a`

3. `f0516a1`

---
name: System最小疎通とGemini fallbackを修正してCD連鎖を完走
date: 2026-09-17T15:39:00
session_id: none
branch: develop
prev: 2026-09-14T17-49-01-develop.md
---

## 1. Summary

Generator Systemの既定topic数を1へ絞って`develop`から`master`へreleaseした。最初のmaster実行でGemini 429後にfallbackしない問題を検出し、TextWriterとTTSの429使い切りを課金Tierに依存しないsource枯渇へ統一した。

修正後はTextWriterがGeminiからCursorへ切り替わり、TTSもprimaryからspareへ切り替わることを実runで確認した。spare側のpaid credit補充後、master refのSystemをmanual dispatchし、Generator System、Playback deploy、Playback E2EのCD連鎖をすべて成功させた。

## 2. Changes

1. Systemのtopic数既定値を1へ変更し、通常UnitとSystem build tagで実行対象を分離した
2. localのStatic・Unit・Race・Integration gateを通し、PR #181で`develop`から`master`へreleaseした
3. PR #181後のSystem失敗から、TextWriterの429分類不足を検出した
4. TextWriterとTTSへFree/Paid共通の429枯渇testをREDで追加し、`ErrSourceExhausted`をwrapする実装へ修正した
5. PR #182をmergeし、master後SystemでTextWriterとTTSのfallback eventを確認した
6. TTSのprimary・spare両方がquota枯渇した実runは失敗し、paid credit補充後の再実行へ切り分けた
7. log用master PRとの二重CDを避けるため途中のrerunをcancelし、logを先送りした
8. master refでSystemをmanual dispatchし、run 35190294909 → 35190594563 → 35190639523の自動連鎖がすべて成功した
9. 各release merge直後に`master`を`develop`へ同期し、最終状態を差分0にした

### Commits

- `a9f5004`
- `e20a22a`
- `8446d31`
- `30518e8`
- `252b63a`
- `af21d0b`

package publickey

// SourceID は Publickey を表す情報源識別子。
const SourceID = "publickey"

// feedBaseURL は Publickey feed の base（placeholder。実 URL は C で確定）。
const feedBaseURL = "https://www.publickey1.jp/atom.xml"
